# docker-compose.yml
version: '3.8'

services:
  backend:
    build:
<<<<<<< Updated upstream
      context: ./src/backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./src/backend:/app
    environment:
      - PYTHONUNBUFFERED=1
      - SISRUA_AUTH_TOKEN=${SISRUA_AUTH_TOKEN:-test-token}
      - SISRUA_CACHE_DIR=/app/cache
      - SENTRY_DSN=${SENTRY_DSN:-}
      - SENTRY_ENVIRONMENT=${SENTRY_ENVIRONMENT:-production}
      - OPENTOPOGRAPHY_API_KEY=${OPENTOPOGRAPHY_API_KEY:-}
      - GROQ_API_KEY=${GROQ_API_KEY:-}
      - REDIS_URL=redis://redis:6379
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    restart: unless-stopped
    depends_on:
      redis:
        condition: service_healthy

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
    restart: unless-stopped

  frontend:
=======
      context: .
      dockerfile: Dockerfile
    container_name: sisrua_backend
    ports:
      - "8000:8000"
    volumes:
      # Mount the source code for live-reloading during development
      - ./src/backend:/app/src/backend
    environment:
      - SISRUA_AUTH_TOKEN=dev-token
      - LOCALAPPDATA=/app/data # Map local app data inside the container
    command: uvicorn backend.api:app --host 0.0.0.0 --port 8000 --reload

  tests:
>>>>>>> Stashed changes
    build:
      context: .
      dockerfile: Dockerfile
<<<<<<< Updated upstream
    ports:
      - "8080:8080"
    depends_on:
      backend:
        condition: service_healthy
    environment:
      - VITE_API_URL=/api
      - VITE_SENTRY_DSN=${VITE_SENTRY_DSN:-}
    restart: unless-stopped
=======
    container_name: sisrua_tests
    command: pytest --cov=backend --cov-report=term-missing
>>>>>>> Stashed changes
