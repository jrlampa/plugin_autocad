"""
backend/domain
Pacote de domínio — camada de compatibilidade e re-exportação.

Expõe os módulos de negócio (GIS, DTOs, geometria) a partir de
um namespace unificado `backend.domain.*` para consumo pelos
testes e pela camada de aplicação.
"""
